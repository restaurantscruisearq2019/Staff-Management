version: "2"
services:
  api:
    image: miacastroco/staff
    restart: always
    ports:
      - "5003:5003"
    volumes:
      - ./api:/api
      - /api/node_modules
    depends_on:
      - mongo
    networks:
      - webappnetwork
    links:
      - mongo
  mongo:
    image: mongo
    restart: always
    container_name: mongo
    volumes:
      - mongodata:/data/db
    ports:
      - 27018:27017
    command: mongod --noauth --smallfiles
    networks:
      - webappnetwork

networks:
  webappnetwork:
    driver: bridge

volumes:
  mongodata:
